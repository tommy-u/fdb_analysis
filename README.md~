# fdb_analysis
Analysis of Forensic Data Base

Will be using a simple supervised learning algorithm (ANN) to make classification or regressions over fields of the DB. An attempt will be made to keep the algorithm as simple as possible as opposed to making esoteric optimizations.

Database:
What happens after 2212?
Use only with age given?
Sex = N?
43->158  L-CX  looks prety complete in cranial
also 254 -266, 314->321

Meta, 
insert row above 1 
calculate num filled in entries of previous col
paste values 1 space to left
sort cols of measurements based on occurance
sorted cols most entries to fewest. 